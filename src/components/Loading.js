import React, { Fragment } from "react";
import { FiLoader } from "react-icons/fi";

const Loading = () => {
  return (
    <Fragment>
      <h1>
        Loading
        <FiLoader />
      </h1>
    </Fragment>
  );
};

export default Loading;
